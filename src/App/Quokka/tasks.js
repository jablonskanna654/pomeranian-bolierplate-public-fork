const person {
    name: "Clyde",
    age: 18,
}
const checkAllowedToDrink = (person) => {
if (age >= 21 && ) {return true}
// else if (typeof name === "strig")_
else {return false}
}
if(checkIfAllowedToDrink(person)) {
    console.log(`${person.name} może pić w barze w USA`)
}
else
 {
}
false